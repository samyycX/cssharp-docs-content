# ListenerNameAttribute 类

## 命名空间
`CounterStrikeSharp.API.Core.Attributes`

## 说明
用于标记监听器委托的名称。

## 构造函数

### ListenerNameAttribute(string name)
创建一个新的ListenerNameAttribute实例。

**参数**:
- `name`: `string` - 监听器的名称

## 属性

### Name
获取监听器的名称。

**类型**: `string`
**访问修饰符**: `public`

## 使用位置
- 委托