# PluginCapability<T> 类

## 命名空间
`CounterStrikeSharp.API.Core.Capabilities`

## 说明
表示插件的一个能力。

## 构造函数

### PluginCapability(string name)
创建一个新的插件能力实例。

**参数**:
- `name`: `string` - 能力的名称

## 属性

### Name
获取能力的名称。

**类型**: `string`

## 方法

### Get()
获取插件的能力值。

**返回值**: `T?` - 能力值,如果未找到则返回默认值 